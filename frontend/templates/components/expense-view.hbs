<div>
  {{!-- MOdificar para que encaje con las expenses --}}
  {{!-- Necesitamos ver los gastos de esta forma? como si fuera un post con comentarios??? --}}

  <h2>{{selectedExpense.type}}</h2> {{i18n 'by'}} {{selectedExpense.owner}}
  <div>{{selectedExpense.content}}</div>
  <h1>MIRA EXPENSE-VIEW.HBS, Y DECIDE QUE PONER AQUI</h1>

  {{!-- No comments on our app --}}
  {{!-- <div id="comments">
    <h3>{{i18n 'Comments'}}</h3>
    {{#each selectedPost.comments}}
      <p>{{author}} commented</p>
      <p>
        {{content}}
      </p>
      <hr>
    {{/each}}
  </div> --}}

  {{!-- {{#if isLogged}}
    <h2>{{i18n 'Leave a comment'}}</h2>
    <div>
      <textarea rows="4" cols="20" id="commentcontent"></textarea>{{commentErrors.content}}
    </div>
    <button id="savecommentbutton">{{i18n 'Save comment'}}</button>
  {{/if}} --}}
</div>
